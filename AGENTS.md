# 小说项目 Codex 工作规则

## 工作区定位

这是一个多本小说共用工作区。根目录只放工作台资料、模板、skills 和各小说项目目录。

## 默认流程

- 每次执行任务前，必须自动判断任务类型，并检索、读取、调用对应的 agent / skill；用户不需要另行提醒。
- 如果任务涉及小说项目，优先使用 `novel-workspace-router` 进行路由，再按任务类型调用对应 skill，例如设定整理、剧情结构、正文草稿、文风维护、审校修订、连续性检查、参考资料或导出。
- 如果任务涉及文风、去 AI 味、审校或修订，必须先读取项目内 `05_修订与审校/` 相关文件，并调用对应的文风或修订 skill。
- 小说相关任务先判断目标项目；目标不明确时先问清楚，不要把内容写到根目录。
- 新建小说项目使用 `_templates/novel-project-template/`。
- 可复用技能源文件维护在 `_skills/`，安装到本机 Codex 后再使用。
- 读书笔记、结构分析和原创项目要分开存放，避免混入正史设定。

## 目录边界

- `_agent/`：只放 agent 规划、流程和验收文档。
- `_skills/`：只放 Codex skill 源文件。
- `_templates/`：只放新小说项目模板。
- 单本小说目录：放该书的设定、剧情、正文、修订、参考和导出。

不要把具体小说正文、设定或读书笔记直接写进 `_agent/`、`_skills/` 或 `_templates/`。

## 多设备同步

- 用 Git 同步本仓库。
- 新设备拉取仓库后运行 `scripts/install-novel-skills.sh` 安装小说 skills。
- 不要提交 Codex 登录缓存、API key、浏览器资料、系统钥匙串或 `.env`。
- 导出包默认不进仓库；需要保留的成品可以单独确认后再提交。
